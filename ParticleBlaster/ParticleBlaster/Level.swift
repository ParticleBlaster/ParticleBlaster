//
//  Level.swift
//  ParticleBlaster
//
//  Created by Bohan Huang on 16/3/17.
//  Copyright © 2017 ParticleBlaster. All rights reserved.
//

import Foundation
/*
class Level: NSObject, NSCoding {
    var objects: [GameObject]
    var score: Int
    var name: String
    
    override init() {
        
    }
    
    /// Conform to NSCoding
    required convenience init(coder aDecoder: NSCoder) {
        let name = aDecoder.decodeObject(forKey: "name") as? String
        let score = aDecoder.decodeObject(forKey: "score") as? String
        // let objects = aDecoder.decodeObject(forKey: "objects") as? [GameObject]
        
        
        if name == nil || score == nil || objects == nil {
            self.init()
        } else {
            self.init(name: name!, score: score!, objects: objects!)
        }
        
    }
    
    func encode(with aCoder: NSCoder) {
        aCoder.encode(name, forKey: "name")
        aCoder.encode(score, forKey: "score")
        // aCoder.encode(objects, forKey: "objects")
    }
}
 */
